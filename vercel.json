{
  "version": 2,
  "builds": [
    {
      "src": "/frontend",
      "use": "@vercel/static-build",
      "config": {
        "buildCommand": "tsc && vite build"
      }
    },
    {
      "src": "/backend/server.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/backend/server.js"
    }
  ]
}
