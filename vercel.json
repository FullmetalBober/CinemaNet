{
  "version": 2,
  "builds": [
    {
      "src": "/frontend",
      "use": "@vercel/static-build",
      "config": {
        "buildCommand": "tsc && vite build"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/backend/server.js"
    }
  ]
}
